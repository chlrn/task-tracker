<configuration>
    <jdbc>
        <driver>org.postgresql.Driver</driver>
        <url>${DATABASE_URL}</url> <!-- Чувствительные данные через переменные окружения -->
        <user>${DATABASE_USER}</user>
        <password>${DATABASE_PASSWORD}</password>
    </jdbc>

    <generator>
        <database>
            <inputSchema>public</inputSchema>
        </database>
        <target>
            <packageName>org.example.jooq.generated</packageName>
            <directory>src/main/java</directory>
        </target>
    </generator>
</configuration>
